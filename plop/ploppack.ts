const packageJsonGeneration = plop => {
	/* TODO: FIll this section out */
};

const folderStructureGeneration = plop => {
	/* TODO: FIll this section out */
};

const packageSelector = plop => {
	/* TODO: FIll this section out */
};

const componentSelector = plop => {
	/* TODO: FIll this section out */
};

module.exports = { packageJsonGeneration, folderStructureGeneration };
